import styled from "styled-components";

const ShowBoard = () => {
  return (
    <Container>
      <div>여기는 게시판 상세페이지 입니다.</div>
    </Container>
  );
};

export default ShowBoard;

const Container = styled.div`
  height: 60vh;
`;
